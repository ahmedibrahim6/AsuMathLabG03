#include <iostream>
#include <string>
#include <fstream>
#include <cstdlib>
#include <string.h>

using namespace std;

int main()
{
	string filename;
	cout<<"Enter the file name \n" ;
	cin>>filename;

    string line_;
    //ifstream if we want to read
    //.c_str() because ifstream can't access string
    ifstream file_(filename.c_str());
    //given true if file is open
    if(file_.is_open())
    {
        while(getline(file_,line_))
        {
            //instead of cout we will access the function that takes the input as string
            cout<<line_<<'\n';
        }
        file_.close();
    }
    else
        cout<<"File is not open\n";

	return 0;
}a
5
10.0
hallo
arwa
